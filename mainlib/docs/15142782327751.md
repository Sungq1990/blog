```
http://stage-api.mifengkong.cn/?test=1&m=HuaXia/getOrderSummaryData&order_id=3131435

http://stage-api.mifengkong.cn/?test=1&m=HuaXia/getOrderData&order_id=3131435
```

```
var len = $(".truncate-by-height").find("span").length
console.warn("总数:"+len)
var ret = '';
for(var i=0;i<len;i++){
	var content = $(".truncate-by-height").find("span")[i].innerHTML;
	content = JSON.parse(content);
	var ext = content.extra;
	ext = JSON.parse(ext);
    ret += content.order_id + "\t" + content.user_id + "\t" + ext.message.trim() + "\n"
}
console.log(ret);
```

```
var len = $(".truncate-by-height").find("span").length
console.warn("总数:"+len)
var ret = '';
for(var i=0;i<len;i++){
    var content = $(".truncate-by-height").find("span")[i].innerHTML;
    content = JSON.parse(content);
    var request_data = content.request_data;
	var biz_data = request_data.biz_data.replace(/<mark>/g,'').replace(/<\/mark>/g,'')
    biz_data = JSON.parse(biz_data);
    ret += biz_data.order_no.trim() + "\t" + (biz_data.remark.trim() == '' ? '无' : biz_data.remark.trim()) + "\n"
}
console.log(ret);
```

